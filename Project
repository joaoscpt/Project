-- // BF V8 FINAL - TRAVA REAL + KILL AURA REAL // MAX 2800 //
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({Name = "BF V8 FINAL | NAO CAI + AURA | 1-2800", LoadingTitle = "Fix queda e aura", Theme = "Amethyst", ConfigurationSaving = {Enabled = false}})

getgenv().AutoFarm = false
getgenv().Bring = true
getgenv().FarmHeight = 15
local Player = game.Players.LocalPlayer

function GetBestWeapon()
    local t = {}
    for _,v in pairs(Player.Backpack:GetChildren()) do if v:IsA("Tool") then table.insert(t, v) end end
    for _,v in pairs(Player.Character:GetChildren()) do if v:IsA("Tool") then table.insert(t, v) end end
    for _,v in pairs(t) do if v.ToolTip == "Melee" then return v.Name end end
    for _,v in pairs(t) do if v.ToolTip == "Sword" then return v.Name end end
    if #t > 0 then return t[1].Name end
    return nil
end

local QuestTable = {
    {L = 0, Mob = "Bandit", Quest = "BanditQuest1", N = 1, Pos = CFrame.new(1060,16,1547)},
    {L = 15, Mob = "Monkey", Quest = "JungleQuest", N = 1, Pos = CFrame.new(-1602,36,152)},
    {L = 60, Mob = "Pirate", Quest = "BuggyQuest1", N = 1, Pos = CFrame.new(-1140,4,3831)},
    {L = 120, Mob = "Desert Bandit", Quest = "DesertQuest", N = 1, Pos = CFrame.new(896,6,4390)},
    {L = 250, Mob = "Chief Petty Officer", Quest = "MarineQuest2", N = 1, Pos = CFrame.new(-5036,28,4324)},
    {L = 300, Mob = "Sky Bandit", Quest = "SkyQuest", N = 1, Pos = CFrame.new(-4842,717,-2623)},
    {L = 375, Mob = "Prisoner", Quest = "PrisonerQuest", N = 1, Pos = CFrame.new(5308,0,474)},
    {L = 650, Mob = "Galley Pirate", Quest = "FountainQuest", N = 1, Pos = CFrame.new(5259,38,4050)},
    {L = 700, Mob = "Raider", Quest = "Area1Quest", N = 1, Pos = CFrame.new(-424,72,1836)},
    {L = 800, Mob = "Swan Pirate", Quest = "Area2Quest", N = 1, Pos = CFrame.new(632,72,918)},
    {L = 1000, Mob = "Zombie", Quest = "ZombieQuest", N = 1, Pos = CFrame.new(-951,172,607)},
    {L = 1100, Mob = "Snow Trooper", Quest = "SnowMountainQuest", N = 1, Pos = CFrame.new(607,401,-5370)},
    {L = 1300, Mob = "Ship Deckhand", Quest = "ShipQuest1", N = 1, Pos = CFrame.new(1037,125,32911)},
    {L = 1350, Mob = "Ship Steward", Quest = "ShipQuest2", N = 1, Pos = CFrame.new(968,125,33245)},
    {L = 1500, Mob = "Pirate Millionaire", Quest = "PiratePortQuest", N = 1, Pos = CFrame.new(-290,44,5580)},
    {L = 2500, Mob = "Isle Outlaw", Quest = "TikiQuest1", N = 1, Pos = CFrame.new(-16539,55,712)},
    {L = 2600, Mob = "Isle Boy", Quest = "TikiQuest2", N = 1, Pos = CFrame.new(-16539,55,712)},
    {L = 2700, Mob = "Sun-kissed Warrior", Quest = "TikiQuest3", N = 1, Pos = CFrame.new(-16539,55,712)},
    {L = 2750, Mob = "Isle Champion", Quest = "TikiQuest3", N = 2, Pos = CFrame.new(-16539,55,712)},
    {L = 2800, Mob = "Tiki Elite", Quest = "TikiQuest4", N = 1, Pos = CFrame.new(-16539,55,712)},
}

function GetQuest()
    local MyLevel = Player.Data.Level.Value
    local Q = QuestTable[1]
    for i = #QuestTable, 1, -1 do if MyLevel >= QuestTable[i].L then Q = QuestTable[i] break end end
    return Q
end

-- KILL AURA LOOP SEPARADO PRA BATER SEM PARAR
spawn(function()
    while task.wait(0.08) do
        if getgenv().AutoFarm then
            pcall(function()
                local HRP = Player.Character:FindFirstChild("HumanoidRootPart")
                if not HRP then return end
                for _,v in pairs(workspace.Enemies:GetChildren()) do
                    if v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 and (v.HumanoidRootPart.Position - HRP.Position).Magnitude < 40 then
                        v.HumanoidRootPart.Size = Vector3.new(60,60,60)
                        v.HumanoidRootPart.CanCollide = false
                        v.Humanoid:ChangeState(14)
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Attack", true)
                        game:GetService("VirtualUser"):ClickButton1(Vector2.new(0,0))
                    end
                end
            end)
        end
    end
end)

local MainTab = Window:CreateTab("Auto Farm 2800", 4483362458)
local Status = MainTab:CreateLabel("MAX 2800 | TRAVADO + AURA")

MainTab:CreateToggle({
   Name = "Auto Farm Level [TRAVA + AURA REAL]",
   CurrentValue = false,
   Flag = "AutoFarmV8",
   Callback = function(Value)
        getgenv().AutoFarm = Value
        local Hum = Player.Character:FindFirstChild("Humanoid")
        if Value then Hum.PlatformStand = true else Hum.PlatformStand = false end

        while getgenv().AutoFarm do task.wait()
            pcall(function()
                local Data = GetQuest()
                local Best = GetBestWeapon()
                Status:Set("Lv."..Player.Data.Level.Value.." | "..Data.Mob.." | "..(Best or "Auto"))

                if not Player.PlayerGui.Main.Quest.Visible then
                    local HRP = Player.Character.HumanoidRootPart
                    if (Data.Pos.Position - HRP.Position).Magnitude > 8000 then HRP.CFrame = Data.Pos * CFrame.new(0,20,0) task.wait(0.5)
                    else
                        game:GetService("TweenService"):Create(HRP, TweenInfo.new((Data.Pos.Position - HRP.Position).Magnitude/280, Enum.EasingStyle.Linear), {CFrame = Data.Pos}):Play()
                        task.wait(1)
                    end
                    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StartQuest", Data.Quest, Data.N)
                end

                for _,Enemy in pairs(workspace.Enemies:GetChildren()) do
                    if Enemy.Name == Data.Mob and Enemy:FindFirstChild("Humanoid") and Enemy.Humanoid.Health > 0 then
                        -- BRING
                        for _,Other in pairs(workspace.Enemies:GetChildren()) do
                            if Other.Name == Data.Mob and (Other.HumanoidRootPart.Position - Enemy.HumanoidRootPart.Position).Magnitude < 400 then
                                Other.HumanoidRootPart.CFrame = Enemy.HumanoidRootPart.CFrame
                                Other.HumanoidRootPart.CanCollide = false
                            end
                        end

                        -- TRAVA EM CIMA - NÃO CAI
                        repeat
                            if not getgenv().AutoFarm or not Enemy.Parent or Enemy.Humanoid.Health <= 0 then break end
                            local HRP = Player.Character.HumanoidRootPart
                            HRP.CFrame = Enemy.HumanoidRootPart.CFrame * CFrame.new(0, getgenv().FarmHeight, 0)
                            HRP.Velocity = Vector3.new(0,0,0)
                            HRP.AssemblyLinearVelocity = Vector3.new(0,0,0)
                            Player.Character.Humanoid:ChangeState(11)

                            local Tool = Player.Backpack:FindFirstChild(Best) or Player.Character:FindFirstChild(Best)
                            if Tool then Player.Character.Humanoid:EquipTool(Tool) end
                            task.wait()
                        until Enemy.Humanoid.Health <= 0 or not getgenv().AutoFarm
                    end
                end
            end)
        end
        Player.Character.Humanoid.PlatformStand = false
   end,
})

MainTab:CreateSlider({Name = "Altura", Range = {8,20}, Increment = 1, Suffix = "Studs", CurrentValue = 15, Flag = "HeightV8", Callback = function(v) getgenv().FarmHeight = v end})
